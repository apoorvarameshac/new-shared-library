@Library('java-shared-library') _

pipeline {
    agent any
    
    stages {
        
	stage('Build Docker Image') {
            steps {
                    buildApp()
		}
        }

        stage('Test In Container') {
            steps {
                    testApp()
                }
        }

        stage('Deploy Container') {
            steps {
                    deployApp()
            }
        }
    }
}

